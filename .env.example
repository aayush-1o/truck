# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Connection (REQUIRED)
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/freightflow?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/freightflow

# JWT Configuration (REQUIRED — server will not start without these)
JWT_SECRET=change_this_to_a_long_random_string_in_production
JWT_EXPIRE=7d

# CORS — comma-separated list of allowed origins
# In production: https://your-domain.com
CORS_ORIGIN=http://localhost:5000,http://localhost:5500

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5000

# Email Configuration (Gmail) — optional, enables password reset & notifications
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password  # Generate at: myaccount.google.com/apppasswords

# Razorpay Payments — optional, enables payment gateway
# RAZORPAY_KEY_ID=rzp_test_your_key_id
# RAZORPAY_KEY_SECRET=your_razorpay_secret

# Geocoding — optional, enables real distance-based pricing (Free tier: 2k req/day)
# Sign up at https://openrouteservice.org to get a free API key
# ORS_API_KEY=your_openrouteservice_api_key
